import type { SVGProps } from "react";
import { SiTypescript } from "react-icons/si";

const TypescriptIcon = (props: SVGProps<SVGSVGElement>) => <SiTypescript {...props} />;

export default TypescriptIcon;
