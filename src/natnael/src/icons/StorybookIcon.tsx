import type { SVGProps } from "react";
import { SiStorybook } from "react-icons/si";

const StorybookIcon = (props: SVGProps<SVGSVGElement>) => <SiStorybook {...props} />;

export default StorybookIcon;
